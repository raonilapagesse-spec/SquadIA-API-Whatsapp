# Token de autenticação da API (header: Authorization: ******
BRIDGE_TOKEN=

# Segredo para assinar os webhooks via HMAC-SHA256
BRIDGE_WEBHOOK_SECRET=

# Porta do servidor (padrão: 8080)
PORT=8080

# Diretório para persistir sessões (padrão: /data/sessions)
BRIDGE_DATA_DIR=/data/sessions
